// Definir una variable
let nombre: string = "Daniela";
let rol: string = "Programadora";
let nivel: number = 19;

// Función para saludar
function saludar(nombre: string, rol: string, nivel: number): string {
  return `Hola, ${nombre}, sé que eres una ${rol} nivel ${19}!`;
}

// Imprimir el saludo
console.log(saludar(nombre, rol, nivel));